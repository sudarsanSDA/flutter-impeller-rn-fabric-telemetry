name: flutter_benchmark
description: Dual-framework empirical benchmark suite - Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '3.5.3'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/list_5k.json
    - assets/data/payload_10k.json
    - assets/images/avatar.png
